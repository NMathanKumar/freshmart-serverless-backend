export const middlewareDescription = {
  authentication: 'JWT validation for protected order service endpoints',
  authorization: 'Role-aware endpoint access controls',
  validation: 'Schema-level input validation for order mutations'
};
