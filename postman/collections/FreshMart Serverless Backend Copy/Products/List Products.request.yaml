$kind: http-request
url: "{{baseUrl}}/products?limit=20&category={{categoryId}}"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
queryParams:
  limit: "20"
  category: "{{categoryId}}"
order: 1000
